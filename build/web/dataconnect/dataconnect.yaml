specVersion: "v1beta"
serviceId: "web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
